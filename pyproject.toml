[project]
name = "toolanything"
version = "0.1.0"
description = "One function to MCP and Tool calling"
authors = [{ name = "ToolAnything" }]
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
